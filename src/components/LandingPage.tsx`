import { motion } from "framer-motion";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import {
  ShieldCheck,
  Brain,
  MapPin,
  Activity,
  Users,
  Zap,
  ArrowRight,
  Camera,
  Mic,
  FileText,
  Gauge,
  TrendingUp,
} from "lucide-react";

const fadeUp = {
  hidden: { opacity: 0, y: 24 },
  show: { opacity: 1, y: 0, transition: { duration: 0.5 } },
};

const stagger = {
  hidden: {},
  show: { transition: { staggerChildren: 0.08 } },
};

const features = [
  {
    icon: Camera,
    title: "AI Image Analysis",
    desc: "Detect potholes, floods, fire, garbage & 20+ hazards automatically from photos.",
    color: "text-emerald-400",
  },
  {
    icon: Brain,
    title: "Severity Prediction",
    desc: "ML models predict Low → Critical severity with confidence scoring.",
    color: "text-cyan-400",
  },
  {
    icon: Zap,
    title: "Auto Department Routing",
    desc: "Smart routing to the correct government department instantly.",
    color: "text-amber-400",
  },
  {
    icon: MapPin,
    title: "Live Safety Heatmap",
    desc: "Real-time interactive map of city hazards with GPS clustering.",
    color: "text-rose-400",
  },
  {
    icon: Users,
    title: "Community Verification",
    desc: "Nearby citizens verify reports, boosting confidence & trust scores.",
    color: "text-violet-400",
  },
  {
    icon: Gauge,
    title: "Safety Score Engine",
    desc: "Every area gets a 0–100 safety score updated in real time.",
    color: "text-teal-400",
  },
];

const stats = [
  { label: "Complaints Resolved", value: "48,219" },
  { label: "Avg Response Time", value: "3.2h" },
  { label: "Active Citizens", value: "1,24,500" },
  { label: "Cities Deployed", value: "27" },
];

const testimonials = [
  {
    name: "Priya Subramanian",
    role: "Municipal Commissioner, Coimbatore",
    quote:
      "SafeNet cut our average complaint resolution time by 64%. The AI routing alone saved hundreds of staff hours.",
  },
  {
    name: "Rahul Mehta",
    role: "DCP Traffic, Indore",
    quote:
      "The predictive hotspot map lets us position patrols before accidents happen. It's a game-changer for city policing.",
  },
  {
    name: "Anjali Rao",
    role: "Citizen Advocate, Bengaluru",
    quote:
      "Finally a platform where my voice matters. I reported a broken streetlight and it was fixed in 18 hours.",
  },
];

const partners = [
  "Municipal Corp.",
  "Traffic Police",
  "Electricity Board",
  "Water Authority",
  "Fire Dept.",
  "Disaster Mgmt",
];

export default function LandingPage({ onEnter }: { onEnter: () => void }) {
  return (
    <motion.div
      variants={stagger}
      initial="hidden"
      animate="show"
      className="relative overflow-hidden"
    >
      {/* Ambient glow */}
      <div className="pointer-events-none absolute -top-40 left-1/2 h-[500px] w-[800px] -translate-x-1/2 rounded-full bg-emerald-500/20 blur-[120px]" />
      <div className="pointer-events-none absolute top-1/3 -right-40 h-[400px] w-[400px] rounded-full bg-cyan-500/10 blur-[100px]" />

      {/* Nav */}
      <motion.nav
        variants={fadeUp}
        className="sticky top-0 z-50 flex items-center justify-between border-b border-white/5 bg-[#0B0F19]/80 px-6 py-4 backdrop-blur-xl md:px-12"
      >
        <div className="flex items-center gap-2.5">
          <div className="flex h-9 w-9 items-center justify-center rounded-xl bg-emerald-400/15 ring-1 ring-emerald-400/30">
            <ShieldCheck className="h-5 w-5 text-emerald-400" />
          </div>
          <span className="text-lg font-bold tracking-tight">
            Safe<span className="text-emerald-400">Net</span>
          </span>
        </div>
        <div className="hidden items-center gap-8 text-sm text-slate-400 md:flex">
          <a href="#features" className="transition hover:text-emerald-400">Features</a>
          <a href="#how" className="transition hover:text-emerald-400">How it Works</a>
          <a href="#map" className="transition hover:text-emerald-400">Live Map</a>
        </div>
        <Button
          onClick={onEnter}
          className="bg-emerald-400 text-[#0B0F19] font-semibold hover:bg-emerald-300"
        >
          Launch Dashboard
        </Button>
      </motion.nav>

      {/* Hero */}
      <section className="relative px-6 pt-20 pb-24 md:px-12 md:pt-32">
        <div className="mx-auto max-w-5xl text-center">
          <motion.div
            variants={fadeUp}
            className="mb-6 inline-flex items-center gap-2 rounded-full border border-emerald-400/20 bg-emerald-400/5 px-4 py-1.5 text-xs font-medium text-emerald-300"
          >
            <span className="relative flex h-2 w-2">
              <span className="absolute inline-flex h-full w-full animate-ping rounded-full bg-emerald-400 opacity-75" />
              <span className="relative inline-flex h-2 w-2 rounded-full bg-emerald-400" />
            </span>
            AI-Powered Smart City Safety Intelligence
          </motion.div>

          <motion.h1
            variants={fadeUp}
            className="font-serif text-4xl font-bold leading-tight tracking-tight md:text-6xl"
          >
            Safer Cities,{" "}
            <span className="bg-gradient-to-r from-emerald-400 to-teal-300 bg-clip-text text-transparent">
              Smarter Response
            </span>
          </motion.h1>

          <motion.p
            variants={fadeUp}
            className="mx-auto mt-6 max-w-2xl text-lg text-slate-400"
          >
            Report, detect, and resolve urban hazards in real time. SafeNet uses
            computer vision, NLP, and predictive ML to keep your city safe.
          </motion.p>

          <motion.div
            variants={fadeUp}
            className="mt-10 flex flex-col items-center justify-center gap-4 sm:flex-row"
          >
            <Button
              onClick={onEnter}
              size="lg"
              className="group bg-emerald-400 text-[#0B0F19] font-semibold hover:bg-emerald-300"
            >
              Enter Dashboard
              <ArrowRight className="ml-2 h-4 w-4 transition group-hover:translate-x-1" />
            </Button>
            <Button
              size="lg"
              variant="outline"
              className="border-white/10 bg-white/5 text-slate-200 hover:bg-white/10"
            >
              <FileText className="mr-2 h-4 w-4" />
              View Demo Report
            </Button>
          </motion.div>
        </div>

        {/* Stats strip */}
        <motion.div
          variants={fadeUp}
          className="mx-auto mt-20 grid max-w-5xl grid-cols-2 gap-4 md:grid-cols-4"
        >
          {stats.map((s) => (
            <div
              key={s.label}
              className="rounded-2xl border border-white/5 bg-white/[0.03] p-5 text-center backdrop-blur-sm"
            >
              <div className="text-2xl font-bold text-emerald-400 md:text-3xl">
                {s.value}
              </div>
              <div className="mt-1 text-xs text-slate-500">{s.label}</div>
            </div>
          ))}
        </motion.div>
      </section>

      {/* How it works */}
      <section id="how" className="px-6 py-20 md:px-12">
        <div className="mx-auto max-w-6xl">
          <motion.div variants={fadeUp} className="mb-14 text-center">
            <h2 className="font-serif text-3xl font-bold md:text-4xl">
              How SafeNet Works
            </h2>
            <p className="mt-3 text-slate-400">
              From citizen report to verified resolution — powered by AI.
            </p>
          </motion.div>

          <div className="grid gap-6 md:grid-cols-4">
            {[
              { step: "01", title: "Report", desc: "Citizen uploads photo, voice, or text with GPS location.", icon: Camera },
              { step: "02", title: "AI Analyze", desc: "Detect issue, predict severity, recommend solutions.", icon: Brain },
              { step: "03", title: "Route", desc: "Auto-assign to correct department & track in real time.", icon: Zap },
              { step: "04", title: "Resolve", desc: "Before/after verification & citizen feedback loop.", icon: ShieldCheck },
            ].map((item, i) => (
              <motion.div key={item.step} variants={fadeUp}>
                <Card className="relative h-full overflow-hidden border-white/5 bg-white/[0.03] backdrop-blur-sm">
                  <div className="absolute right-4 top-4 text-5xl font-bold text-white/5">
                    {item.step}
                  </div>
                  <CardContent className="p-6">
                    <div className="mb-4 flex h-11 w-11 items-center justify-center rounded-xl bg-emerald-400/10 ring-1 ring-emerald-400/20">
                      <item.icon className="h-5 w-5 text-emerald-400" />
                    </div>
                    <h3 className="text-lg font-semibold">{item.title}</h3>
                    <p className="mt-2 text-sm text-slate-400">{item.desc}</p>
                  </CardContent>
                  {i < 3 && (
                    <div className="absolute bottom-6 right-6 text-emerald-400/30">
                      <ArrowRight className="h-5 w-5" />
                    </div>
                  )}
                </Card>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Features */}
      <section id="features" className="px-6 py-20 md:px-12">
        <div className="mx-auto max-w-6xl">
          <motion.div variants={fadeUp} className="mb-14 text-center">
            <h2 className="font-serif text-3xl font-bold md:text-4xl">
              Enterprise-Grade Intelligence
            </h2>
            <p className="mt-3 text-slate-400">
              A full-stack safety platform built for modern cities.
            </p>
          </motion.div>

          <motion.div
            variants={stagger}
            className="grid gap-5 md:grid-cols-2 lg:grid-cols-3"
          >
            {features.map((f) => (
              <motion.div key={f.title} variants={fadeUp}>
                <Card className="group h-full border-white/5 bg-white/[0.03] backdrop-blur-sm transition hover:border-emerald-400/20 hover:bg-white/[0.05]">
                  <CardContent className="p-6">
                    <div className="mb-4 flex h-11 w-11 items-center justify-center rounded-xl bg-white/5 ring-1 ring-white/10 transition group-hover:ring-emerald-400/30">
                      <f.icon className={`h-5 w-5 ${f.color}`} />
                    </div>
                    <h3 className="text-base font-semibold">{f.title}</h3>
                    <p className="mt-2 text-sm leading-relaxed text-slate-400">
                      {f.desc}
                    </p>
                  </CardContent>
                </Card>
              </motion.div>
            ))}
          </motion.div>
        </div>
      </section>

      {/* Live Map Preview */}
      <section id="map" className="px-6 py-20 md:px-12">
        <div className="mx-auto max-w-6xl">
          <motion.div variants={fadeUp} className="mb-14 text-center">
            <h2 className="font-serif text-3xl font-bold md:text-4xl">
              Live Safety Map
            </h2>
            <p className="mt-3 text-slate-400">
              Real-time hazard intelligence across the city.
            </p>
          </motion.div>

          <motion.div variants={fadeUp}>
            <Card className="overflow-hidden border-white/5 bg-white/[0.03]">
              <CardContent className="p-0">
                <div className="relative h-[400px] w-full bg-gradient-to-br from-slate-900 via-[#0B0F19] to-slate-900">
                  {/* Grid lines */}
                  <div
                    className="absolute inset-0 opacity-20"
                    style={{
                      backgroundImage:
                        "linear-gradient(rgba(0,230,118,0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(0,230,118,0.1) 1px, transparent 1px)",
                      backgroundSize: "40px 40px",
                    }}
                  />
                  {/* Heat blobs */}
                  <div className="absolute left-[15%] top-[30%] h-32 w-32 rounded-full bg-rose-500/30 blur-2xl" />
                  <div className="absolute left-[45%] top-[55%] h-40 w-40 rounded-full bg-amber-500/25 blur-2xl" />
                  <div className="absolute left-[70%] top-[25%] h-28 w-28 rounded-full bg-emerald-500/20 blur-2xl" />
                  <div className="absolute left-[60%] top-[70%] h-24 w-24 rounded-full bg-rose-500/20 blur-2xl" />

                  {/* Pins */}
                  {[
                    { x: "18%", y: "35%", c: "bg-rose-500" },
                    { x: "42%", y: "58%", c: "bg-amber-500" },
                    { x: "68%", y: "28%", c: "bg-emerald-500" },
                    { x: "55%", y: "72%", c: "bg-rose-500" },
                    { x: "30%", y: "65%", c: "bg-amber-500" },
                  ].map((p, i) => (
                    <motion.div
                      key={i}
                      className={`absolute flex h-4 w-4 items-center justify-center rounded-full ${p.c} ring-2 ring-white/20`}
                      style={{ left: p.x, top: p.y }}
                      animate={{ scale: [1, 1.3, 1] }}
                      transition={{
                        duration: 2,
                        repeat: Infinity,
                        delay: i * 0.3,
                      }}
                    >
                      <span className={`absolute h-full w-full rounded-full ${p.c} opacity-50`} />
                    </motion.div>
                  ))}

                  <div className="absolute bottom-4 left-4 flex gap-2">
                    {[
                      { c: "bg-emerald-500", l: "Safe" },
                      { c: "bg-amber-500", l: "Moderate" },
                      { c: "bg-rose-500", l: "Critical" },
                    ].map((item) => (
                      <div
                        key={item.l}
                        className="flex items-center gap-2 rounded-lg border border-white/10 bg-[#0B0F19]/80 px-3 py-1.5 text-xs backdrop-blur-sm"
                      >
                        <span className={`h-2 w-2 rounded-full ${item.c}`} />
                        {item.l}
                      </div>
                    ))}
                  </div>
                </div>
              </CardContent>
            </Card>
          </motion.div>
        </div>
      </section>

      {/* Testimonials */}
      <section className="px-6 py-20 md:px-12">
        <div className="mx-auto max-w-6xl">
          <motion.div variants={fadeUp} className="mb-14 text-center">
            <h2 className="font-serif text-3xl font-bold md:text-4xl">
              Trusted by City Leaders
            </h2>
          </motion.div>
          <div className="grid gap-5 md:grid-cols-3">
            {testimonials.map((t) => (
              <motion.div key={t.name} variants={fadeUp}>
                <Card className="h-full border-white/5 bg-white/[0.03] backdrop-blur-sm">
                  <CardContent className="p-6">
                    <p className="text-sm leading-relaxed text-slate-300">
                      "{t.quote}"
                    </p>
                    <div className="mt-5 border-t border-white/5 pt-4">
                      <div className="text-sm font-semibold">{t.name}</div>
                      <div className="text-xs text-slate-500">{t.role}</div>
                    </div>
                  </CardContent>
                </Card>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Partners */}
      <section className="px-6 py-16 md:px-12">
        <motion.div
          variants={fadeUp}
          className="mx-auto max-w-6xl rounded-2xl border border-white/5 bg-white/[0.02] p-8"
        >
          <p className="mb-6 text-center text-sm text-slate-500">
            Integrated with Government Departments
          </p>
          <div className="flex flex-wrap items-center justify-center gap-4">
            {partners.map((p) => (
              <div
                key={p}
                className="rounded-xl border border-white/5 bg-white/[0.03] px-5 py-2.5 text-sm font-medium text-slate-400"
              >
                {p}
              </div>
            ))}
          </div>
        </motion.div>
      </section>

      {/* CTA */}
      <section className="px-6 py-20 md:px-12">
        <motion.div
          variants={fadeUp}
          className="mx-auto max-w-4xl rounded-3xl border border-emerald-400/20 bg-gradient-to-br from-emerald-400/10 to-teal-500/5 p-10 text-center backdrop-blur-sm md:p-16"
        >
          <Activity className="mx-auto mb-4 h-10 w-10 text-emerald-400" />
          <h2 className="font-serif text-3xl font-bold md:text-4xl">
            Ready to make your city safer?
          </h2>
          <p className="mx-auto mt-3 max-w-xl text-slate-400">
            Join 1.2L+ citizens and 27 cities using SafeNet to build safer,
            smarter communities.
          </p>
          <Button
            onClick={onEnter}
            size="lg"
            className="mt-8 bg-emerald-400 text-[#0B0F19] font-semibold hover:bg-emerald-300"
          >
            <TrendingUp className="mr-2 h-4 w-4" />
            Launch Dashboard
          </Button>
        </motion.div>
      </section>

      {/* Footer */}
      <footer className="border-t border-white/5 px-6 py-10 md:px-12">
        <div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-4 md:flex-row">
          <div className="flex items-center gap-2.5">
            <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-emerald-400/15 ring-1 ring-emerald-400/30">
              <ShieldCheck className="h-4 w-4 text-emerald-400" />
            </div>
            <span className="font-semibold">
              Safe<span className="text-emerald-400">Net</span>
            </span>
            <span className="text-sm text-slate-600">© 2025</span>
          </div>
          <div className="flex gap-6 text-sm text-slate-500">
            <a href="#" className="hover:text-emerald-400">Privacy</a>
            <a href="#" className="hover:text-emerald-400">Terms</a>
            <a href="#" className="hover:text-emerald-400">API Docs</a>
            <a href="#" className="hover:text-emerald-400">Contact</a>
          </div>
        </div>
      </footer>
    </motion.div>
  );
}